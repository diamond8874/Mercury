# Agent Commit Log

| Date | Agent ID/Name | Task Summary | Files Changed | Diff Summary | Token Usage (approx) |
|------|---------------|--------------|---------------|--------------|----------------------|
| 2026-07-29 | Meta-Architect Agent | Repository Bootstrap & Setup | `AGENTS.md`, `ARCHITECTURE.md`, `COMMIT.md`, `CHANGELOG.md`, `COMMIT_LOG.md`, `KNOWLEDGE_GRAPH.md` | Created initial metadata and system governance files to minimize context window consumption. | N/A |
| 2026-07-29 | Jules (Software Engineer) | Refactor Monolithic app.py into Modular Architecture | `app.py`, `config.py`, `components/routes.py`, `services/ai_service.py`, `services/data_service.py`, `utils/helpers.py`, `utils/session_manager.py`, `utils/fonts.py`, `utils/job_tracker.py`, `tests/__init__.py`, `tests/test_app.py`, `KNOWLEDGE_GRAPH.md`, `CHANGELOG.md`, `COMMIT_LOG.md` | Modularized configurations, routes/Blueprint, AI & data processing services, helper utility files, session serializations and added a regression test suite for API features. | N/A |
| 2026-07-29 | Claude (Opus 5) | Stage the pipeline: read dataset first, wait for user input, then analyse and plot | `components/routes.py`, `services/profile_service.py` *(new)*, `services/data_service.py`, `services/chart_service.py` *(new)*, `utils/job_tracker.py`, `static/app.js`, `static/index.html`, `static/style.css` | Split upload from analysis. `/api/upload` now saves, parses and spawns `run_background_profile` (semantic typing, nulls, cardinality, correlations, warnings) with **no** model call, returning immediately. `/api/analyze` became asynchronous (`202` + poll, `wait:true` for inline) and its worker blocks on a new `wait_for_profile` handshake before chaining analysis → cleaning → plotting. Added an explicit 8-state job machine to `job_tracker`. Restored chart generation, which was dead code (`charts = []`) in both processing paths: new `chart_service` does AI planning with a deterministic statistical fallback, validates specs against the real DataFrame, emits Chart.js payloads and renders Matplotlib figures for the PDF. Consolidated two divergent cleaning implementations into `apply_actions`. Frontend gained a live dataset read-out card and a single polling loop for all stages. | N/A |
| 2026-07-29 | Claude (Opus 5) | Make the AI layer provider-agnostic | `services/llm_provider.py` *(new)*, `services/ai_service.py`, `components/routes.py`, `config.py`, `static/app.js`, `static/index.html`, `static/style.css`, `requirements.txt`, `.env.example` *(new)* | Replaced the hardcoded NVIDIA endpoint and `z-ai/glm-5.2` model literals (3 call sites) with a 17-provider registry. Keys are auto-detected by prefix; provider, model and base URL resolve explicit → env → registry default. Two transports: the OpenAI SDK for every compatible endpoint, plus a native Anthropic Messages client written over stdlib `urllib` (no new dependency), including SSE streaming. Added a parameter-degradation ladder so providers rejecting `response_format`/`temperature`/`top_p` still succeed. New endpoints `GET /api/providers`, `POST /api/llm/verify`, `POST /api/llm/models`. Settings modal rewritten with provider dropdown, live key detection, model loading and a connection test; legacy `nvidia_api_key` in localStorage is migrated automatically. **Removed a committed live NVIDIA API key.** | N/A |
| 2026-07-30 | Claude (Opus 5) | Make the app deployment ready | `app.py`, `wsgi.py` *(new)*, `config.py`, `utils/job_tracker.py`, `utils/session_manager.py`, `utils/helpers.py`, `components/routes.py`, `static/app.js`, `static/index.html`, `Dockerfile` *(new)*, `docker-compose.yml` *(new)*, `Procfile` *(new)*, `.dockerignore` *(new)*, `.gitignore`, `.env.example`, `requirements.txt`, `tests/test_app.py`, `DEPLOYMENT.md` *(new)* | Converted `app.py` into a `create_app()` factory behind `wsgi:application`. `config.py` is now fully environment-driven (`ENVIRONMENT`, `SECRET_KEY`, `DATA_DIR`, `MAX_UPLOAD_MB`, `RETENTION_DAYS`, `MAX_SESSIONS`, `TRUSTED_PROXY_COUNT`, `CORS_ORIGINS`, `MERCURY_API_TOKEN`), with `validate()` aborting startup on a production process lacking `SECRET_KEY` or running the Werkzeug debugger - previously `app.run(host='0.0.0.0', debug=True)` was an unconditional RCE surface. Added `/api/health` and `/api/ready` probes, JSON handlers for 401/404/405/413/500, security headers, a constant-time shared-secret guard and gated `ProxyFix`. Job state is now mirrored atomically to `<session>.job.json` so status survives restarts and cross-process polls, with `wait_for_profile` re-checking the mirror; session writes became write-then-rename. Added a retention sweep removing expired sessions with all their artifacts. Hardened `parse_json_response` with a string-aware brace scanner so non-OpenAI providers stop falling back to the offline engine on fenced or prose-wrapped JSON. Frontend calls routed through `apiFetch`/`withToken`. Tests 59 -> 73. | N/A |
| 2026-07-30 | Claude (Opus 5) | Responsive UI overhaul | `static/style.css`, `static/index.html`, `static/app.js`, `ARCHITECTURE.md`, `AGENTS.md`, `KNOWLEDGE_GRAPH.md` | Root cause of the reported "stuck, unscrollable" elements: `.step-section` carried `overflow: hidden` while growing, so on short viewports the upload/read-out/goal stack was clipped with no scrollbar and the primary action button was unreachable (measured 670px of content in a 274px box at 900x420). Fixed that and the same class of bug in the settings modal (`max-height: min(88dvh, 900px)` + scrolling body) and `.split-layout` (was `calc(100vh - 120px)`, now flex-sized). Added `min-height: 0`/`min-width: 0` across the whole flex chain so `overflow-y: auto` actually engages. Replaced the single 1024px media query with 11: 1440/1200/1024/820/640/420 plus short-viewport, coarse-pointer, reduced-motion and print. Sidebar becomes an off-canvas drawer below 1024px with backdrop, body scroll lock, Escape-to-close and auto-close on navigation. Grid tracks now `minmax(min(Npx, 100%), 1fr)`; tab strip scrolls horizontally; long data-supplied strings wrap. Verified with an in-browser audit at 1920x1080, 1280x720, 1024x768, 900x420, 820x1180, 390x844 and 360x640: zero clipped-unscrollable elements, zero genuine horizontal overflows. | N/A |
| 2026-07-29 | Claude (Opus 5) | Correctness, security and documentation pass | `services/profile_service.py`, `services/chart_service.py`, `services/data_service.py`, `utils/fonts.py`, `static/app.js`, `tests/test_app.py`, `README.md`, `ARCHITECTURE.md`, `KNOWLEDGE_GRAPH.md`, `AGENTS.md`, `CHANGELOG.md`, `COMMIT_LOG.md`, `API.md` *(new)*, `PROVIDERS.md` *(new)* | Fixed: date-as-text columns misclassified as identifiers (uniqueness heuristic ran before date detection); label encoding wrongly triggered by imputation text containing "category"; `btoa()` crashing the schema grid on non-Latin-1 headers; histograms using `value_counts` instead of binning; target detection preferring a feature over the target in "predict X from Y"; unreachable "Process Data" button; session reloads losing stats/preview/charts; fonts registering only under `__main__`. Escaped all dataset-derived HTML and added an assistant-message tag allowlist. Test suite grown 3 → 45 tests. Authored `API.md` (full route reference, state machine, payload shapes, error codes, cURL walkthrough) and `PROVIDERS.md`; rewrote `README.md`, `ARCHITECTURE.md` and `KNOWLEDGE_GRAPH.md`. | N/A |
